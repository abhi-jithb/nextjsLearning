// Its better to use the client directive only to the Add to the cart button
import React from 'react'
import AddToCart from './AddToCart'

const ProductCard = () => {
  return (
    <div>
     <AddToCart/> 
    </div>
  )
}

export default ProductCard